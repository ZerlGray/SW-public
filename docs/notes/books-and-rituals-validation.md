# Проверка системы книг и обычных умений

Дата: 2026-09-26. После выделения ритуалистики в `codex/rituals` успешно собраны
общая библиотека, клиент, сервер и интеграционный тестовый проект: 0 ошибок.
Использован .NET SDK 9.0.318. Проверка `git diff --check` прошла.
Поиск ссылок на 94 удалённых ритуальных ID в ресурсах, картах и C# ничего не нашёл.
Успешные тесты прежней объединённой версии не считаются проверкой этого среза.

В текущем каталоге 29 знаний: 19 умений, Дешифратор и девять языков.
Целевой набор — девять интеграционных сценариев: пять в `BookAbilityTest`
и четыре в `BookLearningTest`. Ритуальные тесты в него не входят.
Сценарии покрывают обучение и перевод с единственным уроком, ценность оригиналов,
перенос знаний и языков между телами, страницы шифра и перевода, обычные умения.

Результат нового прогона не подтверждён: раннер обнаруживает девять тестов,
но задерживается на первом `DisarmingRiposteIsConsumedByTheFirstCounterHit`.
Первый запуск остановлен после более шести минут без результатов; повторный
завершён диагностикой зависаний по четырёхминутному таймауту. Временный вывод
этапов запуска показал остановку прогресса при инициализации тестового клиента
после `Serializer Types Hash`, до начала проверок способности. Причина задержки
пока не установлена. Диагностическое изменение теста удалено; тесты не отключены.

После сборки клиента, сервера и тестового проекта:

```powershell
dotnet test Content.IntegrationTests/Content.IntegrationTests.csproj --no-build --no-restore --filter "FullyQualifiedName~BookLearningTest|FullyQualifiedName~BookAbilityTest" -- NUnit.NumberOfTestWorkers=1
```

Сохраняется отдельное ограничение: незавершённый
`Tests/Imperial/Medieval/Skills/SkillProgressionTest.cs` со ссылкой на отсутствующее
пространство имён `Content.Server.Imperial.Medieval.Skills.Progression`.
Для этой сборки каталог тестов навыков исключён временным MSBuild target
вне репозитория через `-p:CustomAfterMicrosoftCommonTargets=...`.
Исходные тесты навыков не изменены. Успешная сборка не подтверждает их работу.

Автоматические проверки не заменяют ручной проверки интерфейса и балансного раунда.
Закрытого синтезатора TTS нет в репозитории: для чревовещания он должен брать
позицию из `EntitySpokeEvent.SoundSource`, сохраняя автора в `Source`.
Результаты ритуальной ветки сохранены в `codex/rituals`.
